# {{APP_NAME}}

Vanilla TypeScript Aurobore application (recommended template).

```bash
pnpm install
pnpm build:web
aurobore build
aurobore run
```

Uses `@aurobore/core` and typed plugin imports (`@aurobore/echo`, `@aurobore/device`).
