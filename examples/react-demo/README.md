# React Demo

Пример приложения Aurobore на **React + Vite** (`@aurobore/react`, Echo, Device).

```bash
pnpm install
pnpm build:aurora   # vite build + aurobore build
pnpm aurora:run
```

Создать такой же проект с нуля:

```bash
aurobore create MyApp --template react
```
