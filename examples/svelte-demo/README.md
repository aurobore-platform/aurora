# Svelte Demo

Пример приложения Aurobore на **Svelte 5 + Vite** (`@aurobore/svelte`, Echo, Device).

```bash
pnpm install
pnpm build:aurora
pnpm aurora:run
```

```bash
aurobore create MyApp --template svelte
```
