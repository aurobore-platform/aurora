# Notifications Demo

Демо плагина `@aurobore/notifications` (P4): немедленные и отложенные уведомления, `notification:tap`.

```powershell
pnpm build:web
aurobore build
aurobore run
```

На эмуляторе: **Notify now** показывает уведомление в шторке; tap доставляет событие в WebView.
