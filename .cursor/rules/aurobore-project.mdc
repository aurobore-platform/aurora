---
description: Контекст монорепо Aurobore — этапы MVP, где что лежит, что агент не должен путать
alwaysApply: true
---

# Aurobore — контекст для агента

Монорепо платформы WebView-приложений под **ОС Аврора**. План: `docs/mvp-plan.md`.

## Этапы (кратко)

- **M0** ✅ — PoC моста, инфраструктура JS
- **M0.5** ✅ — dev-toolkit `pnpm poc:*` (`tools/aurora/`)
- **M1** ✅ — runtime-контейнер `runtime/container/`, `pnpm container:*`
- **M2** — Bridge (следующий)
- **M4** — продуктовый CLI `aurobore build/run` (пока **не** реализован)

## Где что лежит

| Путь | Назначение |
|------|------------|
| `packages/*` | JS/TS: core, cli, build, bridge-js |
| `runtime/poc-bridge` | PoC C++/QML (источник; сборка через staging) |
| `runtime/container` | M1 нативный контейнер (ApplicationWindow, asset loader, lifecycle) |
| `tools/aurora/` | Оркестратор эмулятора (`poc.mjs`: poc-bridge + container) |
| `examples_external/` | Локальные клоны внешних референсов (gitignored; `pnpm external:sync`) |
| `docs/aurora/` | Факты SDK, verification-status |
| `docs/alpha-plugins-plan.md` | Доработка A3-плагинов (Camera, Geo, …) с stub до native |

## Native-плагины и Flutter-референс

A3-плагины (Camera, Geolocation, Notifications, Share, Sensors) — пока stub; план доработки:
`docs/alpha-plugins-plan.md`. При реализации native-части: если аналог есть в
[@flutter-community-plugins](https://hub.mos.ru/auroraos/flutter/flutter-community-plugins) —
смотреть **platform-код** (C++/Qt) и логику интеграции с ОС, не копипастить Dart. См.
`docs/dev/native-plugin-guide.md` §Flutter-референс.

## Нативная проверка на эмуляторе

Если задача затрагивает `runtime/**` или проверку на Aurora SDK — см. правило **aurora-dev-toolkit**: используй `pnpm poc:all` (PoC) или `pnpm container:all` (M1), не изобретай цепочку sfdk/scp заново.

## Git

Коммиты и push — **только вручную пользователем**. См. правило **git-workflow**.
