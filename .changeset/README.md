# Changesets

Этот каталог управляется [changesets](https://github.com/changesets/changesets).
Каждое изменение, влияющее на публикуемые пакеты `@aurobore/*`, сопровождается changeset-файлом.

Создать changeset: `pnpm changeset`.
